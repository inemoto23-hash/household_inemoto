# 20260812 アップデート完了記録（母数の見せ方・チャージ元の位置・合計除外・真っ白対策）

- **計画書**: `C:\Users\inemo\.claude\plans\precious-giggling-balloon.md`（合計除外分）
- **ユーザー手動手順**: なし

---

## 1. 真っ白な画面にならないようにした

「PCから開くと使えないページになっている」との報告を受けて調べた。
**原因は特定できていない**が、真っ白になりうる経路が1つ見つかったので塞いだ。

`main.tsx` は MSAL の初期化とリダイレクト処理を待ってから描画していたが、
**`.catch()` が無かった**。ここで失敗すると一度も `render` に到達せず、
何も出ない画面だけが残る。失敗しても必ず描画し、
何が起きたかをサインイン画面に出すようにした。

あわせて `components/ErrorBoundary.tsx` を入れた。React は描画で例外が出ると
画面ごと空にするため、これまでは「使えないページ」としか分からなかった。
今後は原因の文言が画面に出る。

> 位置情報は原因ではない。`useGeo` は記録シートを開いている間と
> 自宅の登録中しか動かず、残高の表示経路には関与しない。
> BE 側の口座問い合わせも SQL を流して正常なことを確認した。

## 2. 予算の母数を画面に出した

「予算タブの配分が未だに母数を変える」との指摘。

コード上は「残りを設定」に変わっており各行の分母も母数だったが、
**画面上部の「配分 ¥…」だけが調整後の合計**を出していた。直し漏れ。

`budgetsGet` の `total` に `baseline` と `adjusted` を足し、上部は
**「予算（母数）」＋調整** を出すようにした。残りを直しても母数は動かない。

## 3. 母数の置き場所は変えていない（文言を直した）

「設定画面の予算tabから予算母数の組み換えが無くなっている」との指摘だったが、
**設定 → 予算タブの「毎月の予算」がその母数**であり、機能は消えていなかった。
文言が実際の動きと合っておらず、母数だと読み取れなかったのが原因。

| 箇所 | 変更 |
|---|---|
| 見出し | 「毎月の予算（既定額）」→ **「毎月の予算（母数）」** |
| 説明 | 「新しい月を開いたときに入ります」→ **「月を締めたときに翌月の母数になります」** |
| 一覧 | 「毎月 ¥133,300」→ **「母数 ¥133,300」** |
| 節の説明 | 「ここで決めた『毎月の予算』が大元の母数です」を追加 |

**説明文が実際の動きから遅れていた。** 月次確定の変更に合わせて直すべきだった。

## 4. チャージ元の位置

シートの下のほうにあり見つけられなかったため、**持ち主のすぐ下**へ移した。

## 5. 合計に含めない印

`20_DATABASE/migrations/016_exclude_from_totals.sql`。
`accounts` と `budget_categories` に `exclude_from_totals` を追加。

### 判断したこと

**アーカイブとは別物にした。**

| | 意味 | 一覧 | 記録で選べるか | 合計 |
|---|---|---|---|---|
| アーカイブ | もう使わない | 消える | 選べない | 入らない |
| 合計に含めない | 使うけれど数えない | 出る | 選べる | 入らない |

混ぜると「使いたいのに隠れる」か「数えたくないのに数えられる」になる。

**外すのは合計だけ。** 分析タブの内訳には出す。何に使ったかは見たいため。

**合計を出している場所を洗い出して潰した。** どこか1つ数え漏らすと
画面ごとに違う数字が出る。対象は口座ページの財布・クレジット合計と、
`budgetsGet` の `total` の2か所。

### 画面

口座とカテゴリの編集シートに「合計に含めない」のチェック。
一覧の行にも `∑ 合計外` と出るので、**開かずにどれが外れているか分かる**。
口座ページには「合計に含めない口座が N 件あります」と添える。
数字が合わないときに理由が画面から分かる状態にする。

## 検証したこと

| 内容 | 結果 |
|---|---|
| マイグレーション 016 | 口座14件・カテゴリ14件に列が付き、既定はすべて 0 |
| **除外の効き方** | 証券2口座に印 → 財布合計 1,223,047 → 748,077。差 474,970 は 403,350 + 71,620 と一致 |
| 予算合計 | 「その他」に印 → 変化なし（配分が 0 のため。整合している） |
| 後片付け | 検証で付けた印はすべて 0 に戻した |
| Function App | デプロイ成功 |
| SWA | `86539a9` → `4402bc0`、`index-BnVGhdqq.js` の配信を確認 |

## 未確認

- **PC で使えなかった件が直ったか。** 原因を特定できていないため、
  再現したら画面に出るエラー文言か、ブラウザの開発者ツールの内容が要る
- 画面からの操作（母数の表示・チャージ元・合計除外のチェック）

## 残課題

| 項目 | 状態 |
|---|---|
| PC で開けない件の根本原因 | 調査中（情報待ち） |
| Azure OpenAI によるあいまい入力 | 未着手 |
| PC 3ペイン / スマホ下部ナビ | 未着手 |
| L2 ドキュメント | 未着手 |
