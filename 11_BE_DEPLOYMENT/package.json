{
  "name": "kakeiflow-api",
  "version": "0.1.0",
  "description": "KakeiFlow バックエンド (Azure Functions / Node 22 / TypeScript v4 モデル)",
  "private": true,
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "deploy": "npm run build && func azure functionapp publish inemoto-KakeiFlow-FC"
  },
  "dependencies": {
    "@azure/functions": "^4.7.0",
    "@azure/identity": "^4.5.0",
    "jose": "^5.9.6",
    "mssql": "^11.0.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/mssql": "^9.1.7",
    "@types/node": "^22.10.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
