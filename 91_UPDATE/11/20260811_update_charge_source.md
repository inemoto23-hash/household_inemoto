# 20260811 アップデート予定（チャージ元の固定）

- **計画書**: `C:\Users\inemo\.claude\plans\precious-giggling-balloon.md`（承認済み）
- **ユーザー手動手順**: なし

## やること

チャージ元は実際には常に同じ口座なので、毎回選ぶのは手間でしかない。
口座に「チャージ元」の属性を持たせ、記録画面ではチャージ先だけを選ぶようにする。

| 対象 | 内容 |
|---|---|
| DB | `015_charge_source.sql` — `accounts.is_charge_source` + 世帯ごとの一意索引 |
| BE | `master.ts`（付け替え）、`accounts.ts` / `bootstrap.ts`（返却） |
| FE | 設定の口座編集にチェック、記録画面はチャージ先だけ選ぶ |
| 文書 | `system_flow.md` / `91_UPDATE/21/` |

## 決めたこと

- チャージ元は**世帯で1つだけ**。電子マネーごとには変えない
- 記録画面では固定表示。ただし「変更」を押せばその場で選べる
- 種別は縛らない。クレジットからも銀行からもチャージするため

## 注意する点

- **1つだけという決まりは DB に守らせる。** フィルタ付き一意索引を張り、
  世帯に2つ立つことが構造的に起きないようにする
- 付け替えは「先に他を落としてから立てる」を1トランザクションで行う。
  落とし忘れれば索引が必ず弾くので、黙って壊れることがない
