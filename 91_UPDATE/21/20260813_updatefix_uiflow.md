# 20260813 アップデート完了記録（予算の見せ方・設定タブ・分析からの明細）

- **計画書**: `91_UPDATE/01/20260813_01_plan.md`
- **予定書**: `91_UPDATE/11/20260813_update_uiflow.md`
- **DB**: 変更なし
- **デプロイ**: BE → FE の順

## 1. 予算の残高を口座と同じ組み方にした

以前は「残り」の下に **12.5px の薄い文字**で消化と予算が左右に散っていて、
`残り = 予算 + 調整 − 消化` という関係が読み取れなかった。

口座画面と同じ「**大きな数字 → 区切り線 → 小さな数字を横並び**」にし、
**予算 / 消化 / 調整**を同じ大きさで並べた。調整が 0 のときは `—` を出す
（¥0 と書くと「調整した結果 0」に見える）。

## 2. 操作をタイルにした

ちょうど4つなので 2×2。`.tile` / `.tile-grid` を `tokens.css` に追加した。
枠線は引かず面の濃淡で示す（配色の方針そのまま）。主となる「残りを設定」だけ
淡いアクセント面＋内側の細い輪にする。

**狭い画面でも2列を保つ。** 縦一列に伸びると下の2つがスクロールに隠れ、
「月を締める」に辿り着けなくなる。

**締め済みのときは2タイル**（履歴 / 締めを戻す）。数を無理に4つに揃えない。
**編集中はタイルを出さない**（操作の途中で他の入口を見せない）。

## 3. 設定のタブを並べ替えた

予算 → 口座 → プール → 定期。実際に触る順に合わせた。

`value` は変えていないので `?tab=accounts` の既存 URL はそのまま動く。
**既定のタブも先頭（予算）に変えた。** 並びを変えたのに開くのが口座のままだと、ちぐはぐになる。

## 4. 分析から明細を開けるようにした

### 一覧シートを共有部品にした

明細の一覧は既に口座と予算の2か所に同じものが書かれていた。分析で3か所目になるので
`features/entries/EntryListSheet.tsx` へ切り出した。編集シートを切り出したときと同じ判断。

画面ごとに違うのは**符号・色・2行目の文言**だけなので、そこだけ渡す形にした。

| 画面 | 符号 | 色 |
|---|---|---|
| 口座 | その口座から見た増減（振替の相手側で反転） | 入=緑 / 出=赤 |
| 予算 | 返金だけ + | **赤で塗らない**（全部が支出なので赤に情報が無い） |
| 分析 | 収入・返金が + | 入=緑 / 出=赤 |

### 場所で絞る道が無かった

`entries` の検索は `from` / `to` / `categoryId` / `accountId` / `q` だけだった。
`placeId` と `merchant` を足した。**BE の追加はこの2つだけ。**

一覧が `COALESCE(表示名, merchant, place_name)` で束ねているので、
絞り込みも同じ2系統にしないと「一覧には出るのに明細が空」が起きる。

### 場所の行だけ操作を分けた

場所の行は 📍地図リンクを抱えているため、**行ごとボタンにできない**
（ボタンの中にリンクは置けない）。場所だけ「🧾 明細」を別に出した。
カテゴリの行は行ごと押せる。

## 検証結果

```
11_BE_DEPLOYMENT   npm run build       通過
10_SITE_DEPLOYMENT npm run typecheck   通過
10_SITE_DEPLOYMENT npm run build       通過
```

**実データで確認**（Entra トークンで Azure SQL へ直接接続）。
分析の一覧の金額と、明細の絞り込み結果が一致するかを上位8件で突き合わせた。

```
OK 産婦人科        一覧=148330(1件) 明細=148330(1件)  経路=placeId
OK 住宅ローン      一覧= 66644(1件) 明細= 66644(1件)  経路=placeId
OK 生鮮市場        一覧= 25312(2件) 明細= 25312(2件)  経路=placeId
OK セキ（鴻巣市）  一覧= 23065(6件) 明細= 23065(6件)  経路=placeId
OK 電気代          一覧= 21265(1件) 明細= 21265(1件)  経路=placeId
OK 西松屋          一覧= 17995(2件) 明細= 17995(2件)  経路=placeId
OK Amazon          一覧= 11485(6件) 明細= 11485(6件)  経路=placeId
OK グラブル        一覧=  9450(2件) 明細=  9450(2件)  経路=placeId
全8件 一致
```

**現状はすべて `placeId` 経路。** マイグレーションで全件がマスタに紐付いたため。
`merchant` 経路は、今後マスタを持たない店（一括登録だけの店）が出たときに効く。

## デプロイ後に確認すること

1. 予算の「残り」から `残り = 予算 + 調整 − 消化` が読み取れる
2. 操作が 2×2 のタイル。締め済みは2タイル。編集中は出ない
3. 設定が 予算 → 口座 → プール → 定期。`?tab=` 無しで予算が開く
4. 分析のカテゴリを押すと明細、店舗の「🧾 明細」で明細。行を押すと編集できる
5. **口座・予算の明細が今までどおり動く**（共有部品への差し替えの退行確認）

## 残課題

| 項目 | 状態 |
|---|---|
| タイルの組み方を他の画面へ広げる | **まず予算画面だけ。見てもらってから判断** |
| 同じ市内に2店舗あると表示名が同じ | `area_detail`（町名）は保存済みで、再取得なしに直せる |
| マスタの編集画面（名前の付け替え・統合・分割） | 未着手 |
| Google Places / AI による名寄せ | 未着手 |

---

## 追記: 支出カテゴリの行が1行だったり2行だったりした

### 原因

行は `[色] [名前] [使用額/母数] [残り]` の4つを `flexWrap: 'wrap'` で並べていた。
そのため**名前の長さと「調整」の有無で折り返す位置が変わり**、
行の高さが揃わなかった。折り返しは入る/入らないで決まるので、
1件ずつ違う結果になる。

### 直し方

口座の行と同じ組み方にした。**折り返さない。**

```
[色] 食費                          ¥7,660  ›
     52,340 / 60,000
[============ バー ============]
```

- 名前と内訳を縦に積み、残りを右に置く
- 入りきらないときは**名前が縮む**（省略記号）
- **内訳も1行に固定した。** 折り返させると、繰越や調整が付いた行だけ背が高くなる。
  入りきらないときは末尾（繰越・調整）から欠けるが、主役の「使った / 母数」は必ず残る

### 確認

Playwright で 320 / 360 / 390px の3幅を測った。長い名前・大きな金額・繰越・調整を
混ぜた5行で、**すべての幅で全行 69px に揃う**ことを確認。
内訳を1行に固定する前は 320px で 1行だけ 85px になっていた。

プールの行も同じ作り（`flexWrap` + `flex: 1 1 160px`）だったので併せて直した。
収入の行とカテゴリ名は、長いときに押し出さないよう省略するようにした。

### 併せて直した名前の衝突

操作タイルに付けた `.tile` が、**口座のアイコン・アイコン選択・設定などが使っている
既存クラスと同名**だった。CSS は後から書いたほうが勝つので、どちらかが必ず崩れる。
`.action-tile` / `.action-grid` に分けた。

**同じファイルの中で名前が衝突していることに、追加時に気付けなかった。**
既存のクラス名を確認せずに増やしたのが原因。
